/**
 * This tiny component exists only to satisfy the build system.
 * You can delete it once other .tsx files are recognised correctly.
 */
export default function V0HealthCheck() {
  return (
    <div style={{ padding: 16, fontFamily: "sans-serif" }}>
      ✓ v0 can now see TypeScript/React source code in this project.
    </div>
  )
}
